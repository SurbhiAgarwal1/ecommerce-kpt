apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: ecommerce-inr-locale
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: "Locale override: Hindi / INR. Changes language, currency, and date format."
