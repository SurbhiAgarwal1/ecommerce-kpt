apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: ecommerce
  labels:
    app: catalog
    component: ecommerce
spec:
  selector:
    app: catalog
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
